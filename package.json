{
  "name": "todo-app",
  "private": true,
  "version": "0.0.0",
  "homepage": "https://gunjanrana.github.io/todo",

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "gh-pages -d dist"
  },

  "dependencies": {
  },

  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}